{
  "compilerOptions": {
    // Specifies the target ECMAScript version for the compiled JavaScript.
    // ES2020 includes modern features like optional chaining and nullish coalescing.
    "target": "ES2020",

    // Specifies the module system to use in the compiled output.
    // CommonJS is used for Node.js environments.
    "module": "CommonJS",

    // Specifies the output directory for compiled JavaScript files.
    "outDir": "./dist",

    // Specifies the root directory of the TypeScript source files.
    // This helps organize the project structure.
    "rootDir": "./src",

    // Enables strict type-checking options for better type safety.
    // Includes options like `noImplicitAny`, `strictNullChecks`, and more.
    "strict": true,

    // Enables interoperability between CommonJS and ES modules.
    // Allows using `import` statements for CommonJS modules.
    "esModuleInterop": true
  },
  // Specifies the files or directories to include in the compilation.
  "include": ["src/**/*"],

  // Specifies the files or directories to exclude from the compilation.
  "exclude": ["node_modules"]
}
